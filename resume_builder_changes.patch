diff --git a/.github/workflows/pipeline.yml b/.github/workflows/pipeline.yml
index fcd18ad..e10c375 100644
--- a/.github/workflows/pipeline.yml
+++ b/.github/workflows/pipeline.yml
@@ -53,16 +53,30 @@ jobs:
           IDS_JSON=$(echo "$IDS_OUTPUT" | grep '^ids_json=' | cut -d= -f2-)
           echo "pending jobs: $IDS_JSON"
 
-          # Step 3: extract job scope for each pending job
+          # Step 3: scrape + clean each pending job's page
           echo "$IDS_JSON" | python -c "
           import json, sys, subprocess
           ids = json.loads(sys.stdin.read())
+          for job_id in ids:
+              print(f'Scraping job page for {job_id}')
+              subprocess.run(['python', '-m', 'src.scrape_job_page', '--job-id', job_id], check=False)
+          "
+
+          # Step 4: get scraped job ids
+          SCRAPED_OUTPUT=$(python -m src.query_by_status --run-id "$RUN_ID" --status scraped)
+          SCRAPED_JSON=$(echo "$SCRAPED_OUTPUT" | grep '^ids_json=' | cut -d= -f2-)
+          echo "scraped jobs: $SCRAPED_JSON"
+
+          # Step 5: extract job scope for each scraped job
+          echo "$SCRAPED_JSON" | python -c "
+          import json, sys, subprocess
+          ids = json.loads(sys.stdin.read())
           for job_id in ids:
               print(f'Extracting job scope for {job_id}')
               subprocess.run(['python', '-m', 'src.extract_job_scope', '--job-id', job_id], check=False)
           "
 
-          # Step 4: get all extracted job ids for stage2
+          # Step 6: get all extracted job ids for stage2
           EXTRACTED_OUTPUT=$(python -m src.query_by_status --run-id "$RUN_ID" --status extracted)
           EXTRACTED_JSON=$(echo "$EXTRACTED_OUTPUT" | grep '^ids_json=' | cut -d= -f2-)
           echo "job_ids=$EXTRACTED_JSON" >> "$GITHUB_OUTPUT"
diff --git a/Supabase setup and config.txt b/Supabase setup and config.txt
index d9fa90c..ac6f191 100644
--- a/Supabase setup and config.txt	
+++ b/Supabase setup and config.txt	
@@ -22,7 +22,8 @@ CREATE TABLE jobs (
   run_id uuid REFERENCES runs(id) ON DELETE CASCADE,
   link text,
   status text NOT NULL DEFAULT 'pending'
-    CHECK (status IN ('pending','extracted','resume_generated','cover_letter_generated','completed','failed')),
+    CHECK (status IN ('pending','scraped','extracted','resume_generated','cover_letter_generated','completed','failed')),
+  cleaned_text text,
   job_scope jsonb,
   resume_json jsonb,
   cover_letter_json jsonb,
@@ -31,4 +32,16 @@ CREATE TABLE jobs (
   error_message text,
   updated_at timestamptz,
   created_at timestamptz NOT NULL DEFAULT now()
-);
\ No newline at end of file
+);
+
+-- MIGRATION: if the jobs table already exists (existing DB), run this instead
+-- of the CREATE TABLE above:
+--
+-- ALTER TABLE jobs ADD COLUMN cleaned_text text;
+-- ALTER TABLE jobs DROP CONSTRAINT jobs_status_check;
+-- ALTER TABLE jobs ADD CONSTRAINT jobs_status_check
+--   CHECK (status IN ('pending','scraped','extracted','resume_generated','cover_letter_generated','completed','failed'));
+--
+-- Note: the exact constraint name (jobs_status_check) may differ if Postgres
+-- auto-generated a different name — check with:
+--   SELECT conname FROM pg_constraint WHERE conrelid = 'jobs'::regclass;
\ No newline at end of file
diff --git a/prompts/prompt1_extract_job_scope.txt b/prompts/prompt1_extract_job_scope.txt
index 0046b61..20ee414 100644
--- a/prompts/prompt1_extract_job_scope.txt
+++ b/prompts/prompt1_extract_job_scope.txt
@@ -4,7 +4,7 @@ Task:
 Extract the job scope from the posting at this URL:
 {job_link}
 
-Source content (HTML or page text):
+Source content (cleaned page text, tags and boilerplate already stripped):
 {page_content}
 
 Return ONLY valid JSON (no markdown, no explanation) that matches this schema intent:
@@ -15,9 +15,9 @@ Return ONLY valid JSON (no markdown, no explanation) that matches this schema in
 - preferred_skills: string[]
 - responsibilities: string[]
 - keywords_for_ats: string[]
-- raw_summary: string
 
 Rules:
 - If unknown, use empty string "" for scalar fields and [] for arrays.
 - Do not invent details that are not in the source.
+- The source text may still contain leftover site chrome (e.g. "Join or sign in", cookie/privacy notices, "Report this job"). Ignore these — they are not part of the job posting.
 - Output JSON only.
\ No newline at end of file
diff --git a/requirements.txt b/requirements.txt
index 47eb46c..b105dd8 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,4 +1,6 @@
 supabase
 requests
 jsonschema
-python-docx
\ No newline at end of file
+python-docx
+beautifulsoup4
+lxml
\ No newline at end of file
diff --git a/schemas/job_scope.schema.json b/schemas/job_scope.schema.json
index 0e0be61..0b24a22 100644
--- a/schemas/job_scope.schema.json
+++ b/schemas/job_scope.schema.json
@@ -22,8 +22,7 @@
     "keywords_for_ats": {
       "type": "array",
       "items": { "type": "string" }
-    },
-    "raw_summary": { "type": "string" }
+    }
   },
   "required": [
     "title",
@@ -32,7 +31,6 @@
     "required_skills",
     "preferred_skills",
     "responsibilities",
-    "keywords_for_ats",
-    "raw_summary"
+    "keywords_for_ats"
   ]
 }
\ No newline at end of file
diff --git a/src/extract_job_scope.py b/src/extract_job_scope.py
index dd2aa55..e328e59 100644
--- a/src/extract_job_scope.py
+++ b/src/extract_job_scope.py
@@ -1,6 +1,5 @@
 import argparse
 import json
-import requests
 
 from src.db import get_job, update_job
 from src.llm_backend import get_backend
@@ -16,13 +15,6 @@ def load_json(path: str):
         return json.load(f)
 
 
-def fetch_job_page(link: str) -> str:
-    # Basic fetch; some sites may block and become failed jobs (by design).
-    resp = requests.get(link, timeout=25, headers={"User-Agent": "Mozilla/5.0"})
-    resp.raise_for_status()
-    return resp.text[:120000]  # keep prompt bounded
-
-
 def main():
     parser = argparse.ArgumentParser()
     parser.add_argument("--job-id", required=True)
@@ -33,17 +25,20 @@ def main():
     if not job:
         raise RuntimeError(f"Job not found: {job_id}")
 
+    cleaned_text = job.get("cleaned_text")
+    if not cleaned_text:
+        raise RuntimeError(f"No cleaned_text found for job {job_id} — run scrape_job_page first")
+
     link = job["link"]
 
     try:
-        page_html = fetch_job_page(link)
         prompt_tmpl = load_text("prompts/prompt1_extract_job_scope.txt")
         schema = load_json("schemas/job_scope.schema.json")
 
         prompt = (
             prompt_tmpl
             .replace("{job_link}", link)
-            .replace("{page_content}", page_html)
+            .replace("{page_content}", cleaned_text)
         )
 
         backend = get_backend("extract")
@@ -57,4 +52,4 @@ def main():
 
 
 if __name__ == "__main__":
-    main()
\ No newline at end of file
+    main()
diff --git a/src/scrape_job_page.py b/src/scrape_job_page.py
new file mode 100644
index 0000000..f6a61db
--- /dev/null
+++ b/src/scrape_job_page.py
@@ -0,0 +1,72 @@
+import argparse
+
+import requests
+from bs4 import BeautifulSoup, Comment
+
+from src.db import get_job, update_job
+
+# Tags that are essentially never useful job-ad content
+NOISE_TAGS = [
+    "script", "style", "svg", "noscript", "iframe",
+    "nav", "footer", "head", "form", "button", "img", "meta", "link",
+]
+
+
+def fetch_job_page(link: str) -> str:
+    resp = requests.get(link, timeout=25, headers={"User-Agent": "Mozilla/5.0"})
+    resp.raise_for_status()
+    return resp.text
+
+
+def clean_html_for_llm(raw_html: str) -> str:
+    """
+    Strip noise tags, comments, and attributes from HTML, then return
+    plain text. Deliberately does NOT do "main content" detection
+    (like trafilatura/readability) since that can drop bullet lists
+    (responsibilities, requirements) that get misjudged as boilerplate.
+    """
+    soup = BeautifulSoup(raw_html, "lxml")
+
+    for tag in soup(NOISE_TAGS):
+        tag.decompose()
+
+    for c in soup.find_all(string=lambda t: isinstance(t, Comment)):
+        c.extract()
+
+    # Attributes (class/id/style/data-*) are most of the token bloat
+    for tag in soup.find_all(True):
+        tag.attrs = {}
+
+    text = soup.get_text(separator="\n")
+    lines = [line.strip() for line in text.splitlines() if line.strip()]
+    return "\n".join(lines)
+
+
+def main():
+    parser = argparse.ArgumentParser()
+    parser.add_argument("--job-id", required=True)
+    args = parser.parse_args()
+
+    job_id = args.job_id
+    job = get_job(job_id)
+    if not job:
+        raise RuntimeError(f"Job not found: {job_id}")
+
+    link = job["link"]
+
+    try:
+        raw_html = fetch_job_page(link)
+        cleaned_text = clean_html_for_llm(raw_html)
+
+        if not cleaned_text.strip():
+            raise RuntimeError("Cleaned text was empty after scraping")
+
+        update_job(job_id, status="scraped", cleaned_text=cleaned_text, error_message=None)
+        print(f"Scraped job page for {job_id} ({len(cleaned_text)} chars)")
+    except Exception as e:
+        update_job(job_id, status="failed", error_message=str(e))
+        print(f"Failed to scrape job {job_id}: {e}")
+
+
+if __name__ == "__main__":
+    main()
diff --git a/test/db_smoke_test.py b/test/db_smoke_test.py
index 743d037..a196408 100644
--- a/test/db_smoke_test.py
+++ b/test/db_smoke_test.py
@@ -76,7 +76,6 @@ def main():
                     "job_scope": {
                         "title": "Smoke Test Title",
                         "company": "Smoke Test Company",
-                        "raw_summary": "DB connectivity test",
                     },
                     "updated_at": datetime.now(timezone.utc).isoformat(),
                 }
